export enum ERoles {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum ESteps {
  USER_INFO = 'USER_INFO',
  ASSET_INFO = 'ASSET_INFO',
  REDEX_FORM = 'REDEX_FORM',
  UPLOAD_FORM = 'UPLOAD_FORM',
  LAST_STEP = 'LAST_STEP',
}

export enum EFuelType {
  SOLAR = 'SOLAR',
  WIND = 'WIND',
  HYDRO = 'HYDRO',
}
